# 東京湾 船釣り釣果パイプライン ランブック

このドキュメントは、Claude Codeのクラウドスケジュールルーティン(RemoteTrigger/routines)が毎日実行する更新手順を定義したものです。このリポジトリ(GitHub: kakeru110/tokyo-bay-fishing)のcloneの上で動作し、Python/Node等の実行環境を使わず、Claude自身のWebFetch/Read/Write/Editツールで完結させます。最後にgit commit & pushしてリポジトリに反映することで、GitHub Pagesで公開している `docs/` の内容が更新されます。

## 全体の流れ
1. `data/sources.json` を読み、各船宿の feed_url / category_url を取得
2. 各ソースについて、WebFetchでRSS(またはHTMLページ)を取得し新着投稿を確認
3. `data/catches.json` に既存の `source_url` と重複しない新規投稿だけを抽出・パース
4. 新規レコードの `ground_text` を `data/fishing_grounds.json` の地名と照合して座標を付与する。実装は `data/species_aliases.json` と同様に読み込みのみで完結させ、スクリプト側に日本語リテラルを埋め込まない(後述の文字化け対策)。
   - `ground_text` が空の場合、まず同じ投稿(同じ funayado + source_url)内の他レコードに `ground_text` が入っていればそれを借りる(1つの釣行で釣り場は基本1箇所のため)。それでも空なら船宿の `port_lat`/`port_lon` にフォールバックし `geocode_match: "fallback_port"`
   - `ground_text` は「〜」「～」「~」「・」のいずれでも複数地名を分割できるようにする(表記ゆれが多い)
   - 各地名は完全一致優先、ダメなら**最長一致のprefix match**で照合する(例:「本牧沖 20m前後」→「本牧沖」に前方一致)。分割後の地名が2つ以上マッチしたら中間点(緯度経度平均)を採用し `geocode_match: "gazetteer_compound"`、1つだけなら `"gazetteer"`
   - 「各堤」「各堤防」「桟橋」等の陸っぱり・桟橋釣りは沖の地名ではないため、無理に登録せず`fallback_port`のままでよい(実質的に港の近くなので妥当)
   - それ以外で本当に未知の沖合地名が新たに出てきた場合は、地理的知識からおおよその座標を `fishing_grounds.json` に追記する(コメントに追加理由を残す)。似た表記ゆれ(例:「剣崎沖」と「剱崎沖」のような異体字)は同じ座標で別キーとして両方登録しておくと後々の照合漏れを防げる
5. 魚種名は `data/species_aliases.json` の別名辞書で正規化する(例:「LTアジ」「ビシアジ」「まだこ」→「マアジ」「マダコ」)。元の表記は `species_raw` に残す。新しい別名パターンに気づいたら辞書に追記する
6. 新規レコードを `data/catches.json` の `records` 配列に追記して保存(既存データは削除・上書きしない。追記のみ)。`(funayado, source_url, species, date, qty_min, qty_max, ground_text)` の組で重複排除する
7. `data/tide.json` の `tide_by_date` に、新規レコードの日付がまだ登録されていなければ追記する。取得元は `https://api.tide736.net/get_tide.php?pc=14&hc=8&yr=YYYY&mn=M&dy=1&rg=month`(神奈川県・走水地点、東京湾内)。`rg=month` は月初から30日分しか返らない仕様なので、31日ある月の31日分だけは `dy=31&rg=day` で個別に補う。取得したJSONの `tide.chart["YYYY-MM-DD"].moon.title`(潮名: 大潮/中潮/小潮/長潮/若潮)と `.moon.age`(月齢)、`tide.chart[...].tide[]` の `cm` 値の max-min(潮位差)を保存する。curlにはUser-Agentヘッダーが必要(`-A "Mozilla/5.0"` 等、無いと403になる)
8. `data/weather.json` の `weather_by_date` に、新規レコードの日付がまだ登録されていなければ追記する。取得元は `https://archive-api.open-meteo.com/v1/archive?latitude=35.25&longitude=139.73&start_date=YYYY-MM-DD&end_date=YYYY-MM-DD&daily=temperature_2m_max,temperature_2m_min,precipitation_sum,windspeed_10m_max,windgusts_10m_max,pressure_msl_mean,weathercode&timezone=Asia%2FTokyo`(認証不要、User-Agent不要)。未来日付は取得できないので、当日〜前日までしか埋まらない点に注意(数日後に再実行すれば埋まる)
9. `data/catches.json` の全件(または直近60〜90日分)から `docs/data.js` を再生成する:
   ```js
   window.GENERATED_AT = "YYYY-MM-DD HH:mm";
   window.SOURCES = { "<id>": {"name": "...", "port": "..."}, ... };  // sources.jsonから生成
   window.WEATHER = { "YYYY-MM-DD": {tmax, tmin, precip_mm, wind_kmh, gust_kmh, pressure_hpa, weathercode}, ... };  // weather.jsonから生成
   window.CATCHES = [ {funayado, date, course, species, size_min, size_max, size_unit, qty_min, qty_max, qty_unit, ground_text, depth_text, lat, lon, geocode_match, source_url, tide_title}, ... ];
   ```
   `tide_title` は `data/tide.json` から日付で引いた潮名(大潮/中潮/小潮/長潮/若潮、無ければ`null`)。`docs/data.js` を書き換えたら `docs/index.html` と `docs/analysis.html` 両方の `data.js?v=N` のキャッシュバスターも1つ上げる
10. 各ソースの取得成否・新規件数・エラーの有無を簡潔に記録する(このファイルの末尾の「実行ログ」節に追記、直近10回分だけ残して古いものは削除してよい)
11. 変更を `git add -A && git commit -m "..." && git push` でリポジトリのmainブランチに反映する(GitHub Pagesが `docs/` を自動で再公開する)。変更が何もなければcommit/pushはスキップしてよい

**注記(2026-08-08時点): 予約可否(空席状況)の自動抽出は廃止**。以前は`data/availability.json`として船宿ごとの空席状況を数時間おきに洗い替えする仕組みを試したが、(a) 16船宿中11船宿でしか拾えず、(b) 船宿ごとに表現形式が全く違い正規化の信頼性が低く、(c) 本質的にパイプラインの実行間隔(数時間おき)より短い周期で古くなる情報だったため、ユーザーの判断でこの自動抽出とその表示機能は削除した。代わりに `docs/funayado.html`(船宿情報ページ、場所・電話番号などほぼ不変の情報のみ)を静的に用意する方針にした。日次パイプラインでこのファイルに触れる必要は無い。

## 個別ソースの注意点(data/sources.jsonのparse_notesと重複するが要点のみ)
- ひらい丸・吉野屋・進丸・宮川丸・伊藤遊船: WordPressのRSS(`/feed/`)が使える。RSSは直近10〜20件程度しか含まれないため、既存データと突き合わせて「まだ取り込んでいない投稿」だけ処理すれば十分(通常は前回実行からの差分のみ)。
- 川崎丸: RSSが不安定なため `/blog/` のHTML一覧を直接取得してパースする。

## エラー時の扱い
- 特定の船宿サイトが落ちている・構造が変わってパースできない場合は、そのソースだけスキップし、他のソースの処理は続行する。スキップした場合はその旨を実行ログに残す。
- 1つのソースでパースに失敗するレコードがあっても、そのレコードだけ捨てて他のレコードは取り込む。

## 新しい船宿を追加する場合
`data/sources.json` の `sources` 配列に追記し、`phase2_candidates` から昇格させる。追加直後の初回実行では、その船宿だけ過去1〜2ヶ月分をバックフィル取得する。

## 実行ログ
(このセクションはスケジュールタスク実行のたびに追記される)
- 2026-08-06(手動実行、日付表示に曜日を追加): ユーザーから「釣果も分析も日付には曜日いれて。2026長いから26でよい。曜日は重要。」と依頼。`docs/common.js`に`fmtDate(dateStr)`ヘルパーを追加(例: `"2026-08-06"` → `"26-08-06(木)"`。`Date.UTC(y,m-1,d)`でUTC基準の日付を組み立てて`getUTCDay()`で曜日を取るようにし、閲覧者のブラウザのタイムゾーン設定に左右されず日本のカレンダー通りの曜日になるようにした)。適用箇所: `docs/app.js`の釣果一覧テーブルの日付セルと地図ポップアップ内の日付表示、`docs/common.js`の`renderFilterSummary`の日付範囲表示(絶対日付指定モード時)、`docs/analysis.js`の日別サイズ/日別数量チャートのX軸ラベルおよび天気チャートのX軸ラベル(`renderSingleSpeciesRangeChart`に`isDate`フラグを追加し、指定時のみ表示用ラベルを`fmtDate`で変換、内部の集計キー・ソートは元のISO文字列のまま扱うことでソート順序への影響を回避)。潮名別チャート(大潮/中潮等、日付ではない)や魚種別サイズチャート(魚種名軸)は対象外のまま維持。テーブルのソート機能(`sortValue('date')`)は引き続き生の`r.date`を比較しており表示フォーマットの変更による影響は無い。Playwrightで曜日の算出結果(2026-08-06→木、2026-08-08→土、2026-08-09→日)とテーブル・フィルター概要・チャートラベル全ての反映を確認。`docs/common.js`をv=4→v=5、`docs/app.js`をv=21→v=22、`docs/analysis.js`をv=3→v=4に更新。
- 2026-08-06(手動実行、統計カードを「好調な船宿」に置き換え): ユーザーから統計カード(釣果報告数/釣り場数/魚種数/直近もっとも報告が多い釣り場/平均サイズ)について「意味あるんだかよくわからない」と指摘。最初に「一番釣れている魚種」カードを提案したが「魚種によって母数が違うので意味ない、平均より多いとかならわかる」と却下され、要望は「どこの船宿が(その船宿にしては)調子がいいか」を相対評価で示すことだと確認。船宿ごとの親切さ・初心者への丁寧さは釣果データからは出せないため別タスクとし、まず「好調な船宿」のみ実装することで合意。`docs/common.js`に`computeHotFunayado(recentRecords)`を追加: (船宿, 魚種, 単位)の組ごとに、サイズ・数量それぞれの平均を「全期間(`CATCHES`全体、ベースライン)」と「表示中の絞り込み(`recentRecords`)」で計算し、ベースライン比+何%かを算出。ベースライン件数5件未満・直近件数2件未満の組は信頼性不足として除外し、ベースラインを上回っている(pct>0)組の中で最大のものを採用(全部下回っていれば「該当なし」を返す、単純な最小マイナスを「好調」と誤表示しないよう明示的にガード)。`docs/index.html`の「釣り場数」カードを`好調な船宿`に置き換え(id: `statHotFunayado`/`statHotFunayadoLabel`、船宿名を数値枠に、`好調(シロギス・数量が平均+91%)`のようなラベルを下に表示)、`docs/app.js`の`render()`から呼び出して反映。Playwrightで確認: `全期間`ではベースラインと直近が同一集合になるため常に「該当なし」になる(正しい挙動)、`直近7日`では実データで「船宿吉野屋 好調(シロギス・数量が平均+91%)」、`直近30日`では同じ組み合わせでも%が下がる(+23%、直近の母数が増えて平均化されるため妥当)ことを確認。「釣り場数」自体の表示は削除したが、地図描画に使う内部集計(`groundGroups`)自体は変更なし。`docs/common.js`をv=5→v=6、`docs/app.js`をv=22→v=23に更新(`docs/index.html`のみ変更、`docs/analysis.html`は共通スクリプトのバージョン番号のみ追随)。次のタスクとして「船宿の親切さ・初心者への丁寧さ」の手動調査が残っている。
- 2026-08-07(自動実行、日次差分取得): main最新化後、16ソース中15ソース(伊藤遊船を除く)に`curl -A "Mozilla/5.0" ...`でアクセスして新着を確認(WebFetchは今回も未使用、egress許可済みのBash curlに統一)。伊藤遊船(itoyusen.com)は`/report/feed/`が引き続き403 Forbiddenで恒久ブロックのためスキップ。川崎丸は`/blog/`が単純なUA指定のみだと今回403を返すようになっており、`Accept`/`Accept-Language`/`Referer`ヘッダーを追加したフルブラウザ風リクエストにしたところ200で取得できた(次回以降もこのヘッダー構成を使うこと)。既存`data/catches.json`との突き合わせの結果、新規取り込み27件(内訳: 吉野屋5・すすむ丸4・つり幸4・ひらいまる3・ひらの丸3・川崎丸2・山下丸2・第三あさなぎ丸1・中山丸1・岩田屋1・平作丸1)。宮川丸は新着候補6件全て前回までに取り込み済み(既存source_urlと一致)または出船予定/営業案内のみで対象外、鈴福丸は新着候補10件(ChokaDetail個別URL)を確認したが全て既存レコード(`/category/Choka/`まとめURLで記録済み)と日付が一致しdedup対象、黒川丸は新着候補1件が「平日限定リレーショート仕立船」の予約受付表のみで釣果記載なし、一郎丸は新着候補1件(ふぐ釣り 08-03)が「お客様見えず休みました」で休船、川崎丸の新着候補6件中4件(51096・51089・51087・51084)は今回も「本日休船」「北東強風予報で出船中止」の告知のみで対象外だった。第三あさなぎ丸の新規タチウオ記録で釣り場「台船西」というgazetteer未登録の地名が出たが、既存の「台船回り」(走水沖と同座標)と同一地点か確証が持てなかったため座標追加はせず`fallback_port`のまま残置(次回以降、同船の投稿で位置が特定できれば追記すること)。座標マッチ内訳(新規27件): gazetteer 11件・gazetteer_compound 2件(いずれも既存gazetteerで解決)・fallback_port 14件。新規species_aliasエイリアス追加・新規gazetteer地名追加はなし。`data/catches.json`は1863→1890件。`data/tide.json`は既に2026-08-30まで格納済みのため追記なし、`data/weather.json`は2026-08-07分(Open-Meteoから取得可能だった最新日)を追加(2026-08-06→08-07)。`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-07 18:05)、`docs/index.html`・`docs/analysis.html`ともにdata.jsキャッシュバスターをv=16→v=17に更新。実行ログはこの回から直近10回分のみ残すよう整理(2026-08-05分の3件と2026-08-06天気データ追加の回を削除)。
- 2026-08-07(手動実行、ナビにアフィリエイトCTAを追加): ユーザーから「あふぃりえいとをもっと踏んでもらいやすくしたい。タイトル下の分析の隣にコンパクトにいれれる?」と依頼。外部のAmazon検索リンクへ直接飛ばすのではなく、`docs/index.html`・`docs/analysis.html`双方の`<nav class="page-nav">`内、タブ2つの右側(`margin-left:auto`)に`🎣 仕掛けを見る`という目立つピル型リンク(`.page-nav-cta`、`var(--accent2)`のオレンジ背景・白文字・角丸999px)を追加し、各ページ内の既存`#gearPanel`(画像付き商品カードのセクション)へのページ内アンカーリンクにした(外部への直リンクだと既に作り込んだ商品カード・画像を素通りしてしまうため、まずサイト内の該当セクションへ誘導する設計とした)。既存の`.page-nav a`スタイルと`.page-nav-cta`の詳細度をクラス2つがけ(`.page-nav a.page-nav-cta`)にすることで`!important`無しで上書き。Playwrightで幅1000px(デスクトップ)・390px・360px(狭いモバイル)いずれもナビバーが折り返し・はみ出しを起こさないことと、CTAクリックで`#gearPanel`まで実際にスクロールすることを確認。`docs/style.css`をv=20→v=21に更新。
- 2026-08-08(手動実行、CTAをナビ内ピルから帯バナーに変更): ユーザーから「他のパターンない?分析にするとなくなる。」と報告。ライブサイトを`curl`で直接確認したところ`analysis.html`にも前項のピル型CTAはHTML上ちゃんと存在しており(GitHub Pagesの`cache-control: max-age=600`によるキャッシュの見え方の問題だった可能性が高い)、あわせて代替パターンとして(A: 現行のナビ内ピル、B: 画面右下固定フローティングボタン、C: ヘッダー直下の全幅帯バナー)の3案を提示したところ「c」を選択。`docs/index.html`・`docs/analysis.html`双方で、ナビ内の`.page-nav-cta`ピル(`</nav>`内)を削除し、代わりに`</header>`直後・`<nav>`より上に全幅の`<a href="#gearPanel" class="gear-banner">🎣 今日のおすすめ仕掛けはこちら →</a>`帯を追加(`var(--accent2)`のオレンジ背景・白文字・中央寄せ・角丸なしでヘッダーとタブ行に挟まれる帯状に)。`docs/style.css`の`.page-nav a.page-nav-cta`ルールを`.gear-banner`/`.gear-banner:hover`に置き換え、モバイル用に`padding`/`font-size`を少し縮める`@media`ルールも追加。Playwrightで両ページともバナーが存在しクリックで`#gearPanel`まで実際にスクロールすることを確認。`docs/style.css`をv=21→v=22に更新。
- 2026-08-08(手動実行、予約可否(空席状況)データの新設): ユーザーから「船宿ごとの予約可否/空き状況をサイトに出したい」と依頼(表示UI自体は別タスク、今回は`data/availability.json`の新設とパイプライン手順の追加まで)。まず既に確認済みだった黒川丸・つり幸・ひらの丸の3船宿について現在の投稿を`curl -A "Mozilla/5.0"`で再取得し、残り13船宿(ひらいまる・吉野屋・進丸・宮川丸・伊藤遊船・川崎丸・第三あさなぎ丸・鈴福丸・中山丸・岩田屋・山下丸・平作丸・一郎丸)のsite_url/category_urlを同様に取得して`予約|空席|空き|満船|受付|出船中止|キャンセル待ち|残りわずか|予定表|定休日|臨時休業`等のキーワードで走査し、日付に紐づく予約可否シグナルの有無を全16船宿で確認した。結果、16船宿中11船宿で近未来(概ね7〜14日以内)の予約可否がパース可能な形で見つかった: 黒川丸(◯△※✖の記号付き予定表、8件)・つり幸(「土曜日出船確定 ご予約締め切り」等の一文、5件)・ひらの丸(「週末の予約(15時現在)」+「8月のお休み」告知、4件)・ひらいまる(トップページの日付×コース別「出船状況」一覧、22件)・平作丸(同様の日付別「出船確定 空席多数・予約受付中」一覧、9件)・宮川丸(「出船確定(現在N名)」の日付リスト、5件)・吉野屋(トップお知らせのスポット出船確定日+臨時休業日、9件)・中山丸(「ぎょさん」テンプレの船長コメント内「★明日は○○船に確定」、直近投稿1件から4エントリ)・川崎丸(釣果投稿の文中に「明日は～ご予約お待ちしています」と1日分だけ、1件)・第三あさなぎ丸(トップの「8月の定休日」告知、1件)・一郎丸(釣果一覧ページの「8月の定休日」告知、3件)。残り5船宿は該当なし: 進丸(「ネット予約が満席でも電話なら空きあり」という一般的な文言のみで日付非紐付け)・伊藤遊船(`/report/feed/`が引き続き403で本文自体取得不可)・鈴福丸(「ぎょさん」テンプレだが船長コメントが釣果の実況のみで将来言及が一切ない、同テンプレでも船宿によって書き方が全く違う点に注意)・岩田屋(`/fishing/schedule/`という専用ページはあるが中身は季節魚カレンダーで空席情報ではなかった)・山下丸(トップに「出船＆イベント予定表」というカレンダーがあるが日付が並ぶだけでイベント/満空のマーカーが無く、当日の釣果ブロックに「カサゴ・ご予約お待ちしております」という曖昧な一文があるのみで将来日付に紐付かないため見送り)。正規化ステータスは`受付中`(予約受付中・空席あり・出船確定)/`残りわずか`(残り僅か・あと1〜2名で確定等、明示的に「わずか」表現がある場合のみ)/`満船`(満員御礼・受付終了・ご予約締め切り)/`出船中止`(出船中止・定休日・臨時休業・配船の都合で休み)/`情報なし`(予備区分、今回は未使用)の5種とし、船宿ごとの記号・フレーズをこれにマッピングした(例: 黒川丸の◯→受付中、△・※→残りわずか、✖→出船中止、受付→受付中。ひらいまるの「あとN名様で出船確定」は不確実性はあるが実質募集継続中のため`受付中`に寄せ、明確に「わずか」と書かれていない限り`残りわずか`にはしなかった)。`data/availability.json`を新規作成し(`checked_at: "2026-08-08 12:30"`、11船宿・計71件)、`docs/data.js`に`window.AVAILABILITY`(`checked_at`と`by_funayado`のみ、CATCHES等は今回未変更)を追記、`docs/index.html`・`docs/analysis.html`双方のdata.jsキャッシュバスターをv=17→v=18に更新(表示UI側の実装は別タスクのため`docs/app.js`等のUIコードは今回変更していない)。PIPELINE.mdの「全体の流れ」に手順9として本手順を追加(既存9〜11は10〜12に繰り下げ)。**今後の実行で必ず守ること**: `availability.json`は釣果と違い**追記ではなく毎回全件洗い替え**(古い日付・古いchecked_atのまま残さない)、その日にシグナルが見つからなかった船宿は`by_funayado`から**キーごと省略**する(前回分のキーを残さない・空配列も置かない)。
- 2026-08-08(手動実行、空き状況の表示UIを実装): 前項で作成した`data/availability.json`/`window.AVAILABILITY`を使い、`docs/index.html`の統計カード行の直下に新規パネル「📅 空き状況(◯◯時点)」を追加(`docs/analysis.html`には追加しない。空き状況は行動喚起型の情報で、過去傾向を見る分析ページより実績ページに合うと判断)。`docs/common.js`に`AVAILABILITY`(`window.AVAILABILITY`のラッパー)を追加してエクスポートに加え、`docs/app.js`に`renderAvailability()`を新設: `AVAILABILITY.by_funayado`の船宿ごとに`date >= 今日(JST)`で最も近い1件(全て過去なら先頭)を選び、日付昇順のチップ一覧として描画。表示は船宿名・正規化ステータス(受付中=緑`var(--sea)`/残りわずか=オレンジ`var(--accent2)`/満船=赤`#c94040`/出船中止=グレー`#94a3b8`のバッジ)・日付(`fmtDate`で曜日付き)+コース名、チップ自体は元記事`source_url`への外部リンクとし、鮮度が命の情報である旨を`checked_at`表示+「予約前に元記事で最新状況をご確認ください」の注記で明示した。既存の船宿チップフィルター(`state.funayado`)と連動させ、チップで船宿を外すと空き状況パネルからも消えるようにした(仕掛けパネルと同様、フィルター条件に追従)。Playwrightで(1)11船宿分のチップが日付昇順で表示されること、(2)船宿チップを1つ解除すると空き状況パネルの件数も1件減ること、(3)コンソールエラーが出ないことを確認。`docs/common.js`をv=6→v=7、`docs/app.js`をv=23→v=24、`docs/style.css`をv=22→v=23に更新。
- 2026-08-08(手動実行、空き状況機能を撤去し船宿情報ページへ方針転換): 実装した「📅 空き状況」パネルを見たユーザーから「なんか違うかも。別ページとかに、船宿情報まとめて。場所と予約方法。全部電話かな？なら電話番号だけでもよい。」とフィードバック。空き状況(16船宿中11船宿でしか拾えず、船宿ごとに表現形式がバラバラで、そもそも数時間で古くなる不安定な情報)を都度スクレイピングして推測するより、場所・電話番号という安定した静的情報を出す方が信頼性・網羅性ともに優れると判断し、AかB(併存)かユーザーに確認したところ「a」(空き状況機能を削除し船宿情報ページに一本化)を選択。`docs/index.html`から空き状況パネル(`#availabilityPanel`)を削除、`docs/app.js`の`renderAvailability`/`availabilityStatusClass`と`render()`内の呼び出しを削除、`docs/common.js`から`AVAILABILITY`のエクスポートを削除、`docs/style.css`から`.availability-*`/`.av-*`関連ルール(デスクトップ・モバイル両方)を削除。`docs/data.js`から`window.AVAILABILITY`ブロックを除去(`window.CATCHES`の閉じ`];`までで打ち切り)。`data/availability.json`を`git rm`で削除。PIPELINE.mdの「全体の流れ」から旧手順9(空き状況抽出)を削除して以降を繰り上げ、廃止した旨と今後代わりに`docs/funayado.html`(船宿情報ページ、場所・電話番号中心の静的ページ)を用意する方針を注記として追加。この時点でキャッシュバスターは変更なし(空き状況表示に使っていたIDのDOM/CSSを削除しただけで、他のUI要素の見た目や振る舞いには影響しないため、直後の船宿情報ページ追加コミットでまとめてバージョンを上げる)。
- 2026-08-08(手動実行、船宿情報ページ`docs/funayado.html`を新設): 前項の方針転換を受け、`data/sources.json`の16船宿全てについて`curl -A "Mozilla/5.0" --compressed ...`で公式サイト(トップページ+必要に応じてアクセス/店舗案内/お問い合わせページ)を取得し、電話番号・住所(乗船場所)・予約方法・受付時間を抽出した。川崎丸は単純なUA指定のみだと403を返したため、前回同様`Accept`/`Accept-Language`/`Referer`付きのフルブラウザ風リクエストで200取得(PIPELINE.mdの既存の注記通り)。伊藤遊船(itoyusen.com)は`http://`/`https://`/`www.`のいずれの組み合わせでも今回も一貫して403 Forbiddenで、複数セッションにまたがる恒久ブロックを再確認したため、公式サイトの代わりに外部の船宿紹介サイト「釣り野郎」に掲載されていた電話番号(090-3336-8808)・住所(千葉県市川市妙典2丁目)を採用し、ページ上に出典が公式サイトでないこと・要確認である旨を目立つ警告注記として明記した(他15船宿はいずれも公式サイト本文から直接確認できた実データで、フォールバック文言や外部情報は使っていない)。品川ひらい丸・ひらの丸の2船宿は電話番号は公式サイトで確認できたが、番地までの正確な住所表記がサイト内に見当たらなかったため、乗船場所の地名(品川浦/富津港)のみを記載し「番地までの記載は見当たりませんでした」と正直に注記(住所を推測・捏造していない)。金沢八景黒川丸は`data/sources.json`上の`port`表記が「横浜・金沢八景」である一方、公式サイトで確認できた実際の住所は「神奈川県横須賀市久里浜7-4-3」で地理的に食い違っていたため、両方をそのままページに併記し注記を添えた(`sources.json`自体の修正は今回のスコープ外につき未実施)。山下丸・一郎丸は電話番号が複数(固定/携帯、TEL/FAX)確認できたためメインの1つをタップ可能な`tel:`リンクにし、残りは補足テキストで併記。予約方法は16船宿とも電話が基本(全て確認済み)で、岩田屋のみ公式サイトにお問い合わせフォームへの導線もあったため併記、第三あさなぎ丸・つり幸は受付時間の記載(8:00〜20:00 / 18時まで)があったため追記した。新規`docs/funayado.html`をエリア別(浦安→船橋市川→木更津→富津→品川→川崎→横浜金沢八景→久里浜→久里浜久比里→横須賀市鴨居の東京湾一周順)にグルーピングして作成、`docs/index.html`・`docs/analysis.html`と同じヘッダー/`<nav class="page-nav">`構成・SEOメタタグパターンを踏襲し、両ページの`<nav>`に3つ目のタブ`📞 船宿情報`を追加(`data-nav-page`付きなので`common.js`の`syncUrlAndNav()`がフィルター状態のクエリ文字列を自動で引き継ぐ、意図通り)。このページ自体はJS不要の静的HTMLのため`data.js`/`common.js`/`app.js`等は読み込んでいない。`docs/style.css`に`.funayado-*`系のカード・グリッドCSSを追加(既存の`.panel`/`.stat-card`のトーンに合わせた配色・角丸)し、`docs/index.html`・`docs/analysis.html`・`docs/funayado.html`3ファイルとも`style.css?v=23→24`に統一。`docs/sitemap.xml`に`funayado.html`のエントリ(priority 0.7、changefreq monthly)を追加。Playwright(Chromium)で、(1)`funayado.html`単体を1000px/390pxで表示しコンソールエラーなし・16船宿カード全件・`tel:`リンクの`href`が正しいことを確認、(2)`index.html`・`analysis.html`をLeaflet/Chart.jsをスタブ化(汎用Proxyで全メソッド呼び出しをチェイン可能にする簡易モック)した上でfileプロトコルで開き、両ページとも3タブ構成になっていること・現在ページに`active`クラスが付くこと・フィルター状態(`?days=30`)が3つ目のタブのリンクにも伝播すること・コンソールエラーが出ないことを確認。スクリーンショットは確認後に削除済み。このページは船宿の連絡先という変化の少ない情報を扱う想定のため、日次の自動パイプライン(手順1〜12)の対象には含めない(`docs/funayado.html`は手動メンテナンス。今後いずれかの船宿が電話番号・移転等で情報を変更した場合は、気づいた時点で本ファイルを手動で再確認・更新すること)。
- 2026-08-08(手動実行、船宿情報ページのコンパクト化+並び順変更): ユーザーから「もう少し縦幅コンパクトにして。順番は神奈川先で東京、千葉にして。」と依頼。`docs/style.css`の`.funayado-*`系ルールを詰め: カードのpadding(14px 16px→7px 10px)・グリッドgap(12px→6px)・セクション間マージン(22px→12px)・各種font-sizeを縮小し、船宿名と港ラベルを`.funayado-head`で1行に横並びにして専有していた1行を削減。電話番号と補足(船長直通・固定/携帯番号)も`.funayado-tel-row`で1行にまとめた。さらに「予約方法: 電話」とだけ書かれていた素の注記(16件中8件: 宮川丸・川崎丸・品川ひらい丸・中山丸・進丸・鈴福丸・平作丸・一郎丸)はページ冒頭の説明文で「多くの船宿は電話予約のみ」と既に述べているため冗長と判断し削除、時間帯・代替連絡手段など固有情報がある注記(吉野屋・岩田屋・つり幸・ひらの丸・第三あさなぎ丸・黒川丸・山下丸)のみ残した(「予約方法: 」という接頭辞も省き本文のみに短縮)。セクション順を東京湾一周順(浦安起点)から神奈川→東京→千葉の都県順に並び替え: 川崎→横浜・金沢八景→久里浜→久里浜・久比里→横須賀市鴨居(神奈川、北から南)→品川(東京)→浦安→船橋・市川→木更津→富津(千葉)。各セクション見出しにも`(神奈川)`/`(東京)`/`(千葉)`を追記して都県が一目でわかるようにし、冒頭の説明文もこの並び順に合わせて更新。Playwrightで16カード・16件の`tel:`リンクが全て維持されていること、セクション見出しの並び順が意図通り(神奈川5・東京1・千葉4の順)であることをデスクトップ(1000px)・モバイル(390px)双方で確認、モバイルではフルページスクリーンショットの縦幅が明らかに詰まっていることも目視確認。`docs/index.html`・`docs/analysis.html`・`docs/funayado.html`3ファイルとも`style.css?v=24→25`に更新。
- 2026-08-08(自動実行、日次差分取得): main最新化後、16ソース中15ソース(伊藤遊船を除く)に`curl --compressed -A "Mozilla/5.0 ..." ...`でアクセスして新着を確認(WebFetchは今回未使用、egress許可済みのBash curlに統一)。伊藤遊船(itoyusen.com)は`/report/feed/`が引き続き403 Forbiddenで恒久ブロックのためスキップ。川崎丸は前回同様`Accept`/`Accept-Language`/`Referer`付きのフルブラウザ風リクエストで200取得。既存`data/catches.json`との突き合わせの結果、**全15ソースで新規0件**だった。内訳: ひらい丸(category一覧の最新375758が既存と一致)・吉野屋(RSSの最新post-944が既存と一致)・進丸(RSSの最新2026-08-07投稿が既存と一致)・川崎丸(最新51128が既存と一致)・つり幸(最新305144が既存と一致)・ひらの丸(最新375751が既存と一致)・中山丸(最新375713が既存と一致)・岩田屋(最新27771が既存と一致)・一郎丸(最新1785825961が既存と一致)は前回取り込み済みの投稿より新しいものが無く新規0件。宮川丸はRSS新着候補5件を確認したがいずれも「ルアータチウオ乗合船出船予定」「出船予定のお知らせ」「８月の営業時間について」「お知らせ(ライフジャケット着用)」等の告知のみ、うち1件(76961.html)は投稿本文中の予約導線リンクへの誤検出で実在の投稿ではなかった。第三あさなぎ丸はRSS新着候補3件のうち2件は「8/1よりスポット太刀魚船開始」の告知のみ、残り1件(07-31イサキ船)は本文に魚種名・数量・釣り場の具体的記載が無い定性的な実況文のみで取り込み対象外。黒川丸(Atom)は新着候補1件が「平日限定リレーショート仕立船」の予約受付＋日程表のみで釣果記載なし。鈴福丸は新着候補10件(ChokaDetail個別URL、2026-07-20〜08-05のビシアジ船)を検出したが、日付・魚種・サイズ・数量を突き合わせた結果、全件が既存レコード(`/category/Choka/`まとめURLで記録済み)と完全一致しdedup対象(前回までに取り込み済み)。座標マッチ内訳: 新規取り込みが無かったため対象外。新規species_aliasエイリアス追加・新規gazetteer地名追加はなし。`data/catches.json`は1890件のまま変化なし。`data/tide.json`は既に2026-08-30まで格納済みのため追記なし、`data/weather.json`は既に前日2026-08-07まで格納済み(当日08-08分はOpen-Meteoの仕様上まだ取得不可)のため追記なし。新規レコードが無いため`docs/data.js`の再生成・キャッシュバスターの更新もスキップ(変更なし)。
