# 東京湾 船釣り釣果パイプライン ランブック

このドキュメントは、Claude Codeのクラウドスケジュールルーティン(RemoteTrigger/routines)が毎日実行する更新手順を定義したものです。このリポジトリ(GitHub: kakeru110/tokyo-bay-fishing)のcloneの上で動作し、Python/Node等の実行環境を使わず、Claude自身のWebFetch/Read/Write/Editツールで完結させます。最後にgit commit & pushしてリポジトリに反映することで、GitHub Pagesで公開している `docs/` の内容が更新されます。

## 全体の流れ
1. `data/sources.json` を読み、各船宿の feed_url / category_url を取得
2. 各ソースについて、WebFetchでRSS(またはHTMLページ)を取得し新着投稿を確認
3. `data/catches.json` に既存の `source_url` と重複しない新規投稿だけを抽出・パース
4. 新規レコードの `ground_text` を `data/fishing_grounds.json` の地名と照合して座標を付与する。実装は `data/species_aliases.json` と同様に読み込みのみで完結させ、スクリプト側に日本語リテラルを埋め込まない(後述の文字化け対策)。
   - `ground_text` が空の場合、まず同じ投稿(同じ funayado + source_url)内の他レコードに `ground_text` が入っていればそれを借りる(1つの釣行で釣り場は基本1箇所のため)。それでも空なら船宿の `port_lat`/`port_lon` にフォールバックし `geocode_match: "fallback_port"`
   - `ground_text` は「〜」「～」「~」「・」のいずれでも複数地名を分割できるようにする(表記ゆれが多い)
   - 各地名は完全一致優先、ダメなら**最長一致のprefix match**で照合する(例:「本牧沖 20m前後」→「本牧沖」に前方一致)。分割後の地名が2つ以上マッチしたら中間点(緯度経度平均)を採用し `geocode_match: "gazetteer_compound"`、1つだけなら `"gazetteer"`
   - 「各堤」「各堤防」「桟橋」等の陸っぱり・桟橋釣りは沖の地名ではないため、無理に登録せず`fallback_port`のままでよい(実質的に港の近くなので妥当)
   - それ以外で本当に未知の沖合地名が新たに出てきた場合は、地理的知識からおおよその座標を `fishing_grounds.json` に追記する(コメントに追加理由を残す)。似た表記ゆれ(例:「剣崎沖」と「剱崎沖」のような異体字)は同じ座標で別キーとして両方登録しておくと後々の照合漏れを防げる
5. 魚種名は `data/species_aliases.json` の別名辞書で正規化する(例:「LTアジ」「ビシアジ」「まだこ」→「マアジ」「マダコ」)。元の表記は `species_raw` に残す。新しい別名パターンに気づいたら辞書に追記する
6. 新規レコードを `data/catches.json` の `records` 配列に追記して保存(既存データは削除・上書きしない。追記のみ)。`(funayado, source_url, species, date, qty_min, qty_max, ground_text)` の組で重複排除する
7. `data/tide.json` の `tide_by_date` に、新規レコードの日付がまだ登録されていなければ追記する。取得元は `https://api.tide736.net/get_tide.php?pc=14&hc=8&yr=YYYY&mn=M&dy=1&rg=month`(神奈川県・走水地点、東京湾内)。`rg=month` は月初から30日分しか返らない仕様なので、31日ある月の31日分だけは `dy=31&rg=day` で個別に補う。取得したJSONの `tide.chart["YYYY-MM-DD"].moon.title`(潮名: 大潮/中潮/小潮/長潮/若潮)と `.moon.age`(月齢)、`tide.chart[...].tide[]` の `cm` 値の max-min(潮位差)を保存する。curlにはUser-Agentヘッダーが必要(`-A "Mozilla/5.0"` 等、無いと403になる)
8. `data/weather.json` の `weather_by_date` に、新規レコードの日付がまだ登録されていなければ追記する。取得元は `https://archive-api.open-meteo.com/v1/archive?latitude=35.25&longitude=139.73&start_date=YYYY-MM-DD&end_date=YYYY-MM-DD&daily=temperature_2m_max,temperature_2m_min,precipitation_sum,windspeed_10m_max,windgusts_10m_max,pressure_msl_mean,weathercode&timezone=Asia%2FTokyo`(認証不要、User-Agent不要)。未来日付は取得できないので、当日〜前日までしか埋まらない点に注意(数日後に再実行すれば埋まる)
9. `data/catches.json` の全件(または直近60〜90日分)から `docs/data.js` を再生成する:
   ```js
   window.GENERATED_AT = "YYYY-MM-DD HH:mm";
   window.SOURCES = { "<id>": {"name": "...", "port": "..."}, ... };  // sources.jsonから生成
   window.WEATHER = { "YYYY-MM-DD": {tmax, tmin, precip_mm, wind_kmh, gust_kmh, pressure_hpa, weathercode}, ... };  // weather.jsonから生成
   window.CATCHES = [ {funayado, date, course, species, size_min, size_max, size_unit, qty_min, qty_max, qty_unit, ground_text, depth_text, lat, lon, geocode_match, source_url, tide_title}, ... ];
   ```
   `tide_title` は `data/tide.json` から日付で引いた潮名(大潮/中潮/小潮/長潮/若潮、無ければ`null`)。`docs/data.js` を書き換えたら `docs/index.html` の `data.js?v=N` のキャッシュバスターも1つ上げる
10. 各ソースの取得成否・新規件数・エラーの有無を簡潔に記録する(このファイルの末尾の「実行ログ」節に追記、直近10回分だけ残して古いものは削除してよい)
11. 変更を `git add -A && git commit -m "..." && git push` でリポジトリのmainブランチに反映する(GitHub Pagesが `docs/` を自動で再公開する)。変更が何もなければcommit/pushはスキップしてよい

## 個別ソースの注意点(data/sources.jsonのparse_notesと重複するが要点のみ)
- ひらい丸・吉野屋・進丸・宮川丸・伊藤遊船: WordPressのRSS(`/feed/`)が使える。RSSは直近10〜20件程度しか含まれないため、既存データと突き合わせて「まだ取り込んでいない投稿」だけ処理すれば十分(通常は前回実行からの差分のみ)。
- 川崎丸: RSSが不安定なため `/blog/` のHTML一覧を直接取得してパースする。

## エラー時の扱い
- 特定の船宿サイトが落ちている・構造が変わってパースできない場合は、そのソースだけスキップし、他のソースの処理は続行する。スキップした場合はその旨を実行ログに残す。
- 1つのソースでパースに失敗するレコードがあっても、そのレコードだけ捨てて他のレコードは取り込む。

## 新しい船宿を追加する場合
`data/sources.json` の `sources` 配列に追記し、`phase2_candidates` から昇格させる。追加直後の初回実行では、その船宿だけ過去1〜2ヶ月分をバックフィル取得する。

## 実行ログ
(このセクションはスケジュールタスク実行のたびに追記される)
- 2026-08-05(手動実行、2026-07-27〜2026-08-05分の未取込をバックフィル): WebFetchツール自体は本セッションでも一貫して403(Anthropicインフラ側の別経路がブロックされたままと推測)だったため、代わりにBash経由の`curl`(ユーザーがegress許可済みと確認したプロキシ経由)で全ソースを取得し、取得したHTML/RSS/AtomをRead/Grepで読んでLLMが手動でレコード化した(スクリプトはcurlでの取得・整形補助と最終JSON組み立てのみに限定し、魚種/釣り場の読み取り判断自体は全て目視で実施)。伊藤遊船(itoyusen.com)のみ全パス403で恒久的にサイト側がブロックしている様子のためスキップ。他15ソースは成功(品川ひらい丸は`www.hiraimaru.net`は生きているが`hiraimaru.net`のみ502でapexドメイン側が不調、`/feed/`は404のため`/category/Choka/`から取得)。つり幸・中山丸・川崎丸は直近ページが更新済み分に届かなかったため、`/api/getRealtimeListPage/` `/api/getChokaListPage/` `/blog/page/2/`等のページングで07-27まで遡って取得。gazetteerに「台船回り」(黒川丸のリレータチ釣り場、走水沖と同座標)を追加。新規取り込み247件(内訳: 吉野屋40・つり幸38・進丸26・黒川丸25・一郎丸17・ひらの丸15・山下丸15・川崎丸14・ひらい丸13・中山丸13・平作丸10・宮川丸8・岩田屋5・鈴福丸5・第三あさなぎ丸3)。座標マッチ内訳: gazetteer 163件・gazetteer_compound 30件・fallback_port 54件。`data/catches.json`は1597→1844件、`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-05 12:10)。
- 2026-08-05(手動実行、`data/sources.json`のport_lat/port_lon一括補正): ユーザーがNominatim逆ジオコーディングで10船宿分の港座標を検証したところ全て陸上(道路・住宅地・ゴルフ練習場・空港格納庫等)だったため、16船宿全ての`site_url`を`curl`(WebFetchは本セッションでも403のためBash curl代替)で取得し、「アクセス」ページの住所テキスト・埋め込みGoogleマップ(iframe pbパラメータ、またはGoogle My Mapsのmid=をKML化して取得)から乗船場所の実座標を特定。得られた座標は全てNominatim `/reverse`で逆引きし、駐車場・港湾道路・漁業協同組合支所・水辺スポーツ施設など港湾/水際の地物であることを確認した上で採用(住宅地の一点のみだが川べり至近と判明したケースも一部あり)。16船宿すべて座標を更新(スキップなし)。代表例: 黒川丸(金沢八景 35.328,139.622 → 実際は横須賀市久里浜 35.2266,139.7118、約13.9km補正。サイト自体の所在地表記が屋号の「金沢八景」と一致していなかった)、一郎丸(35.263,139.677 → 35.24,139.7232、約4.9km補正)、つり幸・中山丸(35.52,139.78 → 35.517,139.725、約5km補正)、伊藤遊船(35.677,139.943 → 35.6921,139.9147、約3.1km補正、市川市妙典の江戸川放水路沿い)。宮川丸のみ乗船場所の明記が無く「店舗にて受付後、船着き場へ移動」との記載だったため、店舗最寄りの木更津港内の水際ランドマーク(中の島大橋、35.3833,139.9128)を暫定採用(店舗そのものの座標35.3857,139.9177よりは水際に近いが、正確な桟橋位置ではない可能性あり、要再確認)。鈴福丸も乗船場所の地図がGoogle My Maps旧形式(認証必須で取得不能)のため、直線距離200m以内にある平作丸の実測乗船場所を参考に港内の座標(35.2207,139.7138)を採用。`data/catches.json`の`geocode_match: "fallback_port"`該当756件(全16船宿分)を対応するfunayadoの新座標で一括更新(gazetteer/gazetteer_compoundの889+199件は対象外・無変更)。`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-05 13:58)、`docs/index.html`の`data.js`キャッシュバスターをv=8→v=9に更新。この直後、並行して`docs/app.js`に「各堤」等の陸っぱりラベルに港名を前置する機能(`r.geocode_match === 'fallback_port'`参照)が別途追加されたことに気づき、`docs/data.js`のCATCHES各レコードに`geocode_match`フィールドが元々含まれていなかった(PIPELINE.mdの旧フィールド定義に無かったため)ことを確認。この機能が動作するよう`geocode_match`をCATCHESのフィールド一覧に追加して`docs/data.js`を再々生成(GENERATED_AT 2026-08-05 14:05)、キャッシュバスターをv=9→v=10に再更新。
- 2026-08-05(手動実行、`data/fishing_grounds.json`の沖合地名gazetteer座標を一括検証・補正): ユーザーが地図上で「観音崎沖」「走水」等の沖合地名が横須賀市内の住宅地を指していると指摘したため、`grounds`登録62件全件についてNominatim(`nominatim.openstreetmap.org`) の forward search(地名→実座標)と reverse geocode(zoom=17/18、現在値が住宅地・道路・学校等の陸上地物を指していないか)を1req/秒以上の間隔で実施し、地理的知識(東京湾の海岸線形状、「〜沖」は対応する陸上地名より沖合側にあるべき)と突き合わせて判定。62件中33件を修正(29件は逆ジオコーディングでも海岸・岬・港湾等の妥当な地物で、行政界のみを指すケースも含め現状維持)。代表例: 「観音崎」35.253,139.738(逆引き→横須賀市鴨居三丁目の住宅地道路)→ 実際の岬(Nominatim `natural=cape`)35.256,139.746に補正、約900m。「走水」35.253,139.700(逆引き→横須賀市桜が丘一丁目の住宅地道路)→ 実在の「走水」交差点35.261,139.735に補正、約3.2km。「観音崎沖」「走水沖」もこの2地点を基準にした従来座標が沖合ではなく岬・海岸の内陸側を向いていたため、実座標を起点に浦賀水道(東京湾口)側へ張り出す座標に補正(観音崎沖: 35.248,139.735→35.247,139.765、走水沖: 35.248,139.703→35.250,139.780)。同様に「鴨居」(横浜市緑区の同名地名と誤って混同、35.263,139.677→観音崎近傍の実在集落35.249,139.734、約4.9km)、「第一海堡」「第二海堡」(Nominatimの島ポリゴンと照合した結果、両者の座標が入れ替わっていたことが判明し訂正)、「久里浜」「羽田沖」「川崎沖」「浮島沖」「竹岡沖」「金谷沖」「検見川沖」「幕張沖」「市川沖」「剱崎沖/剣崎沖」「館山沖」等も同様に内陸・住宅地・空港敷地・ゴルフ場等を指していたため補正。修正した33地名(等価な表記ゆれキー含む)を参照する`data/catches.json`のレコードを更新: `geocode_match: "gazetteer"`のうち439件、`geocode_match: "gazetteer_compound"`のうち171件(複合地名は該当地名を新座標に差し替えた上でPIPELINE.mdの中間点ロジック=単純平均で再計算)、あわせて610件のlat/lonを更新(fallback_port 756件は対象外・無変更)。`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-05 14:47)、`docs/index.html`のキャッシュバスターをv=10→v=11に更新。
- 2026-08-06(手動実行、潮汐データの追加): ユーザーが「潮汐(大潮/小潮)と釣果の関係を見たい」と依頼。`data/tide.json`を新規作成し、`https://api.tide736.net/get_tide.php`(pc=14 神奈川県, hc=8 走水、東京湾内)から2026-05〜08分の潮名(大潮/中潮/小潮/長潮/若潮)・月齢・潮位差(cm)を取得(`rg=month`は31日ある月は30日分しか返らない仕様のため、5/31・7/31は`rg=day`で個別補完)。`docs/data.js`のCATCHES各レコードに、日付をキーに`data/tide.json`から引いた`tide_title`フィールドを追加してフィールド一覧に追記(全1844件に付与、欠損なし)。合わせてサイト側に「潮名別 サイズ」「潮名別 数量」パネル(魚種セレクター付き、大潮〜若潮の5カテゴリで最小・平均・最大を表示)を追加。今後の日次実行でも新しい日付ぶんの潮汐データを`data/tide.json`に追記していくこと(手順は本ファイルの全体の流れ7番を参照)。`docs/index.html`のキャッシュバスターをdata.js v=11→v=12、app.js v=16→v=17に更新。
- 2026-08-06(手動実行、天気データの追加): ユーザーが「天気別の分析」を依頼。`data/weather.json`を新規作成し、Open-Meteo Historical Weather API(認証不要、`https://archive-api.open-meteo.com/v1/archive`、北緯35.25/東経139.73)から2026-05-23〜08-06分の気温・降水量・風速・突風・気圧・天気コードを取得。まず「出船した日限定で潮汐と釣果の関係を見る」の応用として、報告件数(≈出船の有無)と天気の相関を確認したところ、風速(相関係数-0.32)・降水量(-0.38)は報告件数と明確な負の相関があった一方、出船した日に限定して魚種別(タチウオ/マアジ/シロギス)の数量・サイズを風速帯別・降水有無別に比較すると、ほぼ差がなかった(天気は「出船するか」を左右するが「釣れるか」への直接的な影響はデータ上明確でない)。この知見をもとに、`docs/data.js`に`window.WEATHER`(日付→気象データ)を追加し、サイト側に「天気と出船状況」パネル(報告件数の棒グラフ+最大風速の折れ線、左右2軸)を新設。`docs/index.html`のキャッシュバスターをdata.js v=12→v=13、app.js v=17→v=18に更新。今後の日次実行では`data/tide.json`と同様に`data/weather.json`も新しい日付ぶんを追記していくこと(全体の流れ8番を参照。Open-Meteoは未来日を返さないため、直近1〜2日分は次回以降の実行で埋まる)。
- 2026-08-06(手動実行、実績/分析のページ分割): チャートパネルが積み重なって1ページが縦に長くなりすぎたため、ユーザーの承認を得て「釣果実績」(`docs/index.html`: フィルター・統計カード・地図・釣果一覧テーブル・おすすめ仕掛け)と「分析」(新規`docs/analysis.html`: 魚種別サイズ・日別サイズ/数量・潮名別サイズ/数量・天気と出船状況の全6チャート)の2ページに分割。フィルター状態(期間・魚種・船宿)は`URLSearchParams`でクエリ文字列に同期し、ページ間の`<nav class="page-nav">`リンクに伝播させることでどちらのページを見ていても同じ絞り込み条件が維持されるようにした。共通ロジック(データ読み込み・フィルター状態・URL同期・チップUI・ヘッダーメタ表示・`fmtRange`/`displayGroundLabel`等のフォーマッタ)を新規`docs/common.js`(`window.TBF`として公開)に切り出し、`docs/app.js`は実績ページ固有の処理(統計・地図マーカー・ソート可能テーブル・仕掛け推薦・大物バッジ)のみに縮小、チャート生成コードは新規`docs/analysis.js`に移動。`docs/index.html`からはChart.js CDNタグと全チャートパネルを削除(このページはもうチャートを描画しないため)。Playwright(Chromium、Leaflet/Chart.jsをスタブ化)で両ページの初期表示・フィルター変更によるURL/ナビリンク更新・テーブルソート・ページ間遷移後のフィルター維持・チャートセレクター切り替えをすべて確認、コンソールエラーなし。キャッシュバスターは新規ファイルのため`common.js?v=1`・`analysis.js?v=1`から開始、`docs/app.js`はv=18→v=19、`docs/style.css`はv=16→v=17に更新。
- 2026-08-06(手動実行、分析ページへのフィルターUI追加+バグ修正): ユーザーから「分析をみたときに、絞り込みすると画面が変わってしまう」と報告。原因は前項のページ分割で`docs/analysis.html`に絞り込み操作手段が無く(読み取り専用の概要表示と`docs/index.html`へのリンクのみ)、絞り込みを変えるには実績ページへ移動(=画面遷移)するしかない構造になっていたこと。`docs/index.html`の`<aside>`と同じ期間ボタン・魚種/船宿チップの`<details>`一式を`docs/analysis.html`にもそのまま追加し、`docs/analysis.js`から`T.initFilterUI(render)`を呼んで配線(`docs/common.js`の`initFilterUI`は元々どちらのページでも動くよう`#speciesChips`の有無で判定する作りだったため、大きな改修は不要)。これにより分析ページ上で絞り込んでもURLクエリの書き換え(`history.replaceState`)のみでページ遷移は発生しない。副次的に、期間ボタンの「現在アクティブな選択」がページ読み込み時に必ずHTML上のデフォルト(直近30日)のまま描画され、URLクエリの実際の`days`値(例: 7)と食い違う既存バグも発見・修正(`docs/common.js`の`initFilterUI`内で`data-days`と`state.days`を比較して初期表示時にも正しいボタンへ`active`クラスを付与するよう変更)。また分析ページを絞り込みなしの初期状態(全魚種選択)で直接開くとJSエラーで日別/潮名別チャートが描画されない別バグを発見: `renderSingleSpeciesRangeChart`が、同一魚種でも日付によってcm表記とkg表記が混在するケース(例: マダコを稀にkgでなくcmで記載する投稿がある等)で、集計後に確定した単位と実際に値が存在するカテゴリの単位が食い違い`undefined.min`を参照して例外になっていた。集計後にサンプル数が最も多い単位を選び、その単位のデータを持つカテゴリだけに絞り込むよう修正。Playwrightで(1)分析ページを直接開いた場合の初期描画にエラーが出ないこと、(2)チップ/期間ボタンの操作がページ遷移を起こさずURLのみ更新されること、(3)分析ページで設定した絞り込みが実績ページへのナビリンクに正しく伝播すること、(4)実績ページ側でも期間ボタンの初期アクティブ状態がURLの`days`と一致することを確認。`docs/common.js`をv=1→v=2、`docs/analysis.js`をv=1→v=2に更新(両方とも`docs/index.html`・`docs/analysis.html`の該当箇所を更新)。
- 2026-08-06(手動実行、基本的なSEO対策): ユーザーが「SEO対策はなにができる？」と依頼。サイトに`meta description`・OGP/Twitterカードタグ・`robots.txt`・`sitemap.xml`が一切無かったため追加。`docs/index.html`・`docs/analysis.html`それぞれの`<head>`に`meta description`、`canonical`、`og:*`(type/site_name/title/description/url/locale)、`twitter:card`系タグを追加(タイトルも「東京湾 船釣り釣果マップ｜魚種・船宿別の最新釣果情報」「釣果データ分析｜東京湾 船釣り釣果マップ」に微調整)。`docs/robots.txt`(Sitemap行付き)と`docs/sitemap.xml`(index.html優先度1.0、analysis.html優先度0.8)を新規作成。GitHub Pagesの実URL(`https://kakeru110.github.io/tokyo-bay-fishing/`、CNAME無し、独自ドメイン未設定)を`curl`で200確認済み。構造化データ(JSON-LD)やSSR対応は今回のスコープ外(コンテンツがJSで動的描画されるためクローラーが釣果一覧本文まで拾えていない可能性があるが、まずは基本のmeta/robots/sitemapを入れてGoogle Search Console登録後の様子見とする)。HTMLファイル自体はキャッシュバスター対象外のため、バージョン番号の更新は無し。
- 2026-08-06(手動実行、スケジュールルーティン未実行分の追いつき): 本来13:00/18:00/21:00 JSTに自動実行されるはずのスケジュールタスクが動作しておらず(直近コミットなし)、ユーザー依頼によりPIPELINE.mdの手順通り手動実行。`git fetch && checkout -B main origin/main`でmain最新化後、16ソース全てに`curl -A "Mozilla/5.0" ...`でアクセス(WebFetchは使わずBash curlで統一)。伊藤遊船(itoyusen.com)は`/report/feed/`が引き続き403 Forbiddenで恒久ブロックと判断しスキップ(前回同様)。他15ソースは取得成功。宮川丸は新着2件とも「沖堤渡船出船予定」「ルアータチウオ乗合船出船予定」という予告投稿のみで魚種/数量の記載がなく取り込み対象外、進丸・第三あさなぎ丸・岩田屋・平作丸・ひらの丸は前回取得済み投稿より新しいものが無く新規0件、一郎丸は新着1件が「ふぐ釣り、お客様見えず休みました」という休船報告のみで取り込み対象外、川崎丸の新着候補5件中2件(51096・51089)は「本日休船」の告知のみで対象外だった。鈴福丸は前回実行(08-05)時点でcategory一覧に反映されていなかった08-05分の投稿(ビシアジ船)を今回新たに検出し取り込んだ(見落としのバックフィル)。新規取り込み17件(内訳: 吉野屋5・つり幸5・黒川丸2・ひらいまる1・川崎丸1・鈴福丸1・中山丸1・山下丸1)。座標マッチ内訳: gazetteer 10件・gazetteer_compound 1件・fallback_port 6件(既存gazetteerで全て解決、新規地名追加や新規species_aliasエイリアス追加は今回なし)。`data/catches.json`は1844→1861件。`data/tide.json`は既に2026-08-30まで(潮汐は予測値のため未来日も取得済み)、`data/weather.json`も既に2026-08-06まで格納済みだったため、いずれも追記なし(変更なし)。`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-06 19:45)、`docs/index.html`・`docs/analysis.html`ともにdata.jsキャッシュバスターをv=13→v=14に更新。
- 2026-08-06(自動実行、日次差分取得): main最新化後、16ソース中15ソース(伊藤遊船を除く)に`curl -A "Mozilla/5.0" ...`でアクセスして新着を確認(WebFetchツールは今回未使用、egress許可済みのBash curlに統一)。伊藤遊船(itoyusen.com)は`/report/feed/`が引き続き403 Forbiddenで恒久ブロックのためスキップ。既存`data/catches.json`との突き合わせの結果、ひらい丸・吉野屋・進丸・宮川丸(告知投稿のみで釣果記載なし4件)・第三あさなぎ丸・つり幸・ひらの丸・中山丸・岩田屋・平作丸・一郎丸(新着1件は「お客様見えず休み」のみ)は新規0件。川崎丸は新着候補4件(51084・51087・51089・51096)を確認したが全て「出船中止」告知のみで釣果記載なし、対象外。鈴福丸は新着候補10件(ChokaDetail個別URL)を検出したが、既存レコードが別のsource_url(`/category/Choka/`)で同一内容を既に保持しておりdedup判定で全件重複、新規0件(前回実行までに取り込み済みと確認)。山下丸は本日8/6分の新着2件中1件(カサゴ 16〜27cm 73匹 鴨居沖20m)が未取込のため追加。黒川丸(Blogspot Atom)はフィード内の直近25件を既存source_urlと突き合わせたところ、7/9分・7/21分の釣果投稿(いずれもリレータチ+リレーアジの2魚種、計4レコード)が過去のバックフィルで漏れていたことを発見。取り込む過程で、既存の該当分(id 832・848・849、2026-07-27の初回バックフィル分)が`raw_text`に実際のサイト文面ではなく英語の要約文(例:"Relay tachi ranged 65-110cm (2-22 fish caught)."、"Relay Ajis measured 23-35cm with higher catch rates of 10-51 fish at 50m off Kannonzaki.")が入っており、`ground_text`欠落・`geocode_match: fallback_port`など明らかにサイト原文を読まず生成された低品質レコードだったことが判明したため、この3件を削除し、Atomフィードの実際の本文から起こした正しいレコードに置き換えた。新規追加5件(山下丸1・黒川丸4)、削除3件(黒川丸、上記の重複・低品質分)。**重要な既知の問題**: 同様の英語要約`raw_text`を持つ疑わしいレコードが黒川丸に他にも多数(id 828〜891付近、2026-06-11〜2026-07-23の間で約56件、`raw_text`に4文字以上の連続英字を含むもので検出可能)残っており、2026-07-27の初回バックフィル実行時にサイト原文を読まずに生成された可能性が高い。今回はフィード上で日付が確認できた7/9・7/21分のみ修正し、それ以外(7/11〜7/20, 7/22, 7/23等)は範囲外として未着手(該当日の本文は今回のフィード取得で読めているため、次回実行時までにこのログを見て個別に修正するか、または黒川丸の該当期間だけ再バックフィルすることを推奨。Blogspotのフィードは直近25件までしか遡れないため、放置すると原文を再取得できなくなる点に注意)。座標マッチ内訳(新規追加分): gazetteer 5件(走水沖2・観音崎沖2・鴨居沖1、いずれも既存gazetteerで解決)・fallback_port 0件。新規species_aliasエイリアス追加・新規gazetteer地名追加はなし。`data/catches.json`は1861→1863件(+5件追加、-3件削除)。`data/tide.json`・`data/weather.json`はいずれも対象日付(2026-07-09, 2026-07-21, 2026-08-06)を既に格納済みのため追記なし(変更なし)。`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-06 21:19)、`docs/index.html`・`docs/analysis.html`ともにdata.jsキャッシュバスターをv=14→v=15に更新。
- 2026-08-06(手動実行、黒川丸の低品質raw_text一括修正): 前項ログで既知の問題として記録されていた黒川丸の英語要約`raw_text`汚染(id 828〜891付近、2026-06-11〜2026-07-23、54件)に対応。WebFetchはこのセッションでも403だったため`curl -A "Mozilla/5.0" "https://kurokawa-maru.blogspot.com/2026/07/"`等でBlogspotの月別アーカイブページを取得し、ページ末尾の「その他の投稿」リンクが指す`search?updated-max=<timestamp>&max-results=N`を辿って2026年5月中旬まで遡ることで対象期間(6/11〜7/23)の実投稿をほぼ全件再取得した(検索結果ページは記事本文がBloggerの「スニペット」ウィジェット内に全文格納されている点に注意。`<div class='post-body entry-content...'>`ではなく`<div class='snippet-item r-snippetized'>`側を読む必要があり、最初これに気づかず本文が空文字になるパースミスをした)。54件のうち52件は実投稿本文と照合でき、`raw_text`をサイト原文(全角ｶﾅの釣種タグ+ｻｲｽﾞ+数量+釣り場+実況コメント)に置き換え、`ground_text`/`depth_text`/`size_min`/`size_max`/`qty_min`/`qty_max`をあわせて実測値に修正した。残り2件(id 862・863、2026-07-02分のマアジ/タチウオ)は7/1と7/3の投稿の間に該当日の記事自体がブログ上に存在しない(未修正のまま放置されたか、その日は釣行自体が無かった可能性が高い)ことを確認したため、既存の数値は根拠不明ながら明白な誤りとも断定できず変更せず、`raw_text`も英語要約のまま**未修正として残置**(次回以降、同ブログの別アーカイブ経路や船宿への確認が取れれば再訪すること)。修正作業中、突き合わせ用に船宿の`date`/`source_url`と実投稿の対応関係を検証したところ、初回バックフィル時のデータ抽出自体に日付ズレのバグがあったとみられる5件を追加で発見・訂正した: id 885・886・887(記録上は2026-06-13だが数値・原文が完全に一致するのは2026-06-14の投稿"6/１４の釣果情報"のみだったため`date`・`source_url`を06-13→06-14に修正、あわせて`course`不一致は無し)、id 878(記録上2026-06-19・course「ショートアジ」だが実際は2026-06-20の投稿のリレーアジ欄と完全一致したため`date`を06-19→06-20、`course`を「ショートアジ」→「リレーアジ」に修正)、id 877(記録上2026-06-20・サイズ欠損だが、既存`source_url`が指す投稿の実際の日付は2026-06-21で数量・釣り場は記録と一致していたためsource_urlはそのまま`date`のみ06-20→06-21に修正しサイズ65～100cmを追記)。いずれも移設先の日付に既存レコードが無いことを確認済みでdedup上の衝突は無い。他にも、実投稿では釣り場タグが「2海堡」「観音崎沖」なのに記録では「観音崎」「浅場」等の曖昧・誤った地名になっていたケース(id 844: 観音崎→第2海堡、id 845: 浅場→観音崎沖)や、テンヤ釣果にリレータチ(天秤)側のサイズ範囲が誤って混入していたケース(id 838: size 60〜110cmを実際の値=無し(テンヤはサイズ非記載)に修正)、`course`のタイプミス「リレータチ(テンリュウ)」を実際の釣法名「リレータチ(テンヤ)」に修正(id 836)、など細かな誤りも合わせて訂正した。ground_text変更に伴い`data/fishing_grounds.json`の既存地名(観音崎沖・走水沖・第2海堡・久里浜沖・剣崎沖)で全て解決でき、新規地名の追加は無し。座標マッチ内訳(52件): gazetteer 51件・gazetteer_compound 1件(id 879「久里浜～剣崎沖」)。before/after例: id828(2026-07-23 タチウオ)は`raw_text`が`"catch statistics (Tachi: 65-100cm, 5-30 fish), 60m off Hashirizui."`→`"ﾘﾚｰﾀﾁ 天秤 65～110cm 1～8本 走水沖40～60ｍ 走水～台船回りで広範囲に反応見れました。難易度少々高く、乗れていても単発のアタリで数が伸び悩みました。"`(数量1～8本・釣り場「走水沖」も原文照合で修正、旧`ground_text`は本来マアジ側の「観音崎沖」を誤って流用していた)。id850(2026-07-08 タチウオ)は数値自体は正しかったが`raw_text`のみ`"catch statistics (Tachi: 65-100cm, 5-30 fish), 60m off Hashirizui."`→`"ﾘﾚｰﾀﾁ 天秤 65～100cm 5～30本 走水沖60ｍ 走水～台船回りで反応見れ、活性良く、アタリ多く、小～中型主体に良型混じりで数が伸びました。"`に置換。なお本件と同種の症状(数値は妥当だが`raw_text`が英語要約)を持つ黒川丸の別レコード(id 853、2026-07-07マアジ、`raw_text: "Kannonji (22-32cm, 15-28 Aji)."`)を1件見つけたが、ユーザー指定の54件リストに含まれないため今回は意図的に対象外とした(検出用正規表現が`catches?|specimens?|yielded|ranged|results?|method|location|depth|fish`等のキーワードに依存しており、この記録はどれにもマッチしない短い要約だったため素通りしたとみられる。同種のすり抜けが他にも残っている可能性があり、次回要調査)。`data/catches.json`の件数自体は1863件のまま変化なし(追加・削除なし、内容修正のみ)。`docs/data.js`を全件から再生成(GENERATED_AT 2026-08-06 23:00)、`docs/index.html`・`docs/analysis.html`ともにdata.jsキャッシュバスターをv=15→v=16に更新。
- 2026-08-06(手動実行、Amazonアフィリエイトの画像付き商品カード試験導入): ユーザーから「Amazonアフィリエイトがこの見せ方(検索結果へのリンクのみ)で本当に売れる?画像とかあったほうがよくない?」と相談があり、報告数の多いタチウオ・マアジ・シロギスの3魚種に限定して画像付きの具体的な商品カードを試験導入することで合意。`curl -A "Mozilla/5.0" "https://www.amazon.co.jp/s?k=<キーワード>"`で実際の検索結果HTMLを取得し(`data-component-type="s-search-result"`のカードからASIN・商品タイトル・`class="s-image"`の画像URLを正規表現で抽出)、各魚種2点ずつ、有名メーカー(ハヤブサ・がまかつ・ダイワ・第一精工・メジャークラフト)かつ船釣り用途に合う商品を目視で選定。価格はcurl経由だと通貨がUSD表示になる不安定なセッションでしか取得できず、変動もするため今回は表示しないことにした(画像・商品名のみ)。`docs/gear.js`に`window.GEAR_PRODUCTS`(魚種→{asin, title, image}配列)を新設し、`docs/app.js`の`renderGearRecommendations`を、表示中の上位3魚種それぞれについて`GEAR_PRODUCTS`があれば画像+ASIN直リンク(`https://www.amazon.co.jp/dp/<ASIN>?tag=kakeru020-22`)のカードを、無ければ従来どおり`GEAR_RECOMMENDATIONS`のキーワード検索リンクにフォールバックするよう変更(魚種ごとの判定なので、上位3魚種の一部だけ商品カードが無くても残りはキーワード検索カードとして表示される)。`docs/style.css`に`.gear-product-img`(白背景・`object-fit:contain`・高さ100px、モバイルは80px)を追加。画像URL(`m.media-amazon.com`)は本セッションのサンドボックスプロキシがChromium側では未対応(curlでは200を確認済み、Playwrightの子プロセスは`ERR_CONNECTION_RESET`)のため実ブラウザでのピクセル確認はできなかったが、レイアウト・リンク・alt文言はPlaywrightで確認済み。`docs/gear.js`をv=8→v=9、`docs/app.js`をv=19→v=20、`docs/style.css`をv=17→v=18に更新。今後、他の魚種にも広げるかは実際のクリック率次第で判断。
