# 東京湾 船釣り釣果パイプライン ランブック

このドキュメントは、Claude Codeのクラウドスケジュールルーティン(RemoteTrigger/routines)が毎日実行する更新手順を定義したものです。このリポジトリ(GitHub: kakeru110/tokyo-bay-fishing)のcloneの上で動作し、Python/Node等の実行環境を使わず、Claude自身のWebFetch/Read/Write/Editツールで完結させます。最後にgit commit & pushしてリポジトリに反映することで、GitHub Pagesで公開している `docs/` の内容が更新されます。

## 全体の流れ
1. `data/sources.json` を読み、各船宿の feed_url / category_url を取得
2. 各ソースについて、WebFetchでRSS(またはHTMLページ)を取得し新着投稿を確認
3. `data/catches.json` に既存の `source_url` と重複しない新規投稿だけを抽出・パース
4. 新規レコードの `ground_text` を `data/fishing_grounds.json` の地名と照合して座標を付与する。実装は `data/species_aliases.json` と同様に読み込みのみで完結させ、スクリプト側に日本語リテラルを埋め込まない(後述の文字化け対策)。
   - `ground_text` が空の場合、まず同じ投稿(同じ funayado + source_url)内の他レコードに `ground_text` が入っていればそれを借りる(1つの釣行で釣り場は基本1箇所のため)。それでも空なら船宿の `port_lat`/`port_lon` にフォールバックし `geocode_match: "fallback_port"`
   - `ground_text` は「〜」「～」「~」「・」のいずれでも複数地名を分割できるようにする(表記ゆれが多い)
   - 各地名は完全一致優先、ダメなら**最長一致のprefix match**で照合する(例:「本牧沖 20m前後」→「本牧沖」に前方一致)。分割後の地名が2つ以上マッチしたら中間点(緯度経度平均)を採用し `geocode_match: "gazetteer_compound"`、1つだけなら `"gazetteer"`
   - 「各堤」「各堤防」「桟橋」等の陸っぱり・桟橋釣りは沖の地名ではないため、無理に登録せず`fallback_port`のままでよい(実質的に港の近くなので妥当)
   - それ以外で本当に未知の沖合地名が新たに出てきた場合は、地理的知識からおおよその座標を `fishing_grounds.json` に追記する(コメントに追加理由を残す)。似た表記ゆれ(例:「剣崎沖」と「剱崎沖」のような異体字)は同じ座標で別キーとして両方登録しておくと後々の照合漏れを防げる
5. 魚種名は `data/species_aliases.json` の別名辞書で正規化する(例:「LTアジ」「ビシアジ」「まだこ」→「マアジ」「マダコ」)。元の表記は `species_raw` に残す。新しい別名パターンに気づいたら辞書に追記する
6. 新規レコードを `data/catches.json` の `records` 配列に追記して保存(既存データは削除・上書きしない。追記のみ)。`(funayado, source_url, species, date, qty_min, qty_max, ground_text)` の組で重複排除する
7. `data/tide.json` の `tide_by_date` に、新規レコードの日付がまだ登録されていなければ追記する。取得元は `https://api.tide736.net/get_tide.php?pc=14&hc=8&yr=YYYY&mn=M&dy=1&rg=month`(神奈川県・走水地点、東京湾内)。`rg=month` は月初から30日分しか返らない仕様なので、31日ある月の31日分だけは `dy=31&rg=day` で個別に補う。取得したJSONの `tide.chart["YYYY-MM-DD"].moon.title`(潮名: 大潮/中潮/小潮/長潮/若潮)と `.moon.age`(月齢)、`tide.chart[...].tide[]` の `cm` 値の max-min(潮位差)を保存する。curlにはUser-Agentヘッダーが必要(`-A "Mozilla/5.0"` 等、無いと403になる)
8. `data/weather.json` の `weather_by_date` に、新規レコードの日付がまだ登録されていなければ追記する。取得元は `https://archive-api.open-meteo.com/v1/archive?latitude=35.25&longitude=139.73&start_date=YYYY-MM-DD&end_date=YYYY-MM-DD&daily=temperature_2m_max,temperature_2m_min,precipitation_sum,windspeed_10m_max,windgusts_10m_max,pressure_msl_mean,weathercode&timezone=Asia%2FTokyo`(認証不要、User-Agent不要)。未来日付は取得できないので、当日〜前日までしか埋まらない点に注意(数日後に再実行すれば埋まる)
9. `data/catches.json` の全件(または直近60〜90日分)から `docs/data.js` を再生成する:
   ```js
   window.GENERATED_AT = "YYYY-MM-DD HH:mm";
   window.SOURCES = { "<id>": {"name": "...", "port": "..."}, ... };  // sources.jsonから生成
   window.WEATHER = { "YYYY-MM-DD": {tmax, tmin, precip_mm, wind_kmh, gust_kmh, pressure_hpa, weathercode}, ... };  // weather.jsonから生成
   window.CATCHES = [ {funayado, date, course, species, size_min, size_max, size_unit, qty_min, qty_max, qty_unit, ground_text, depth_text, lat, lon, geocode_match, source_url, tide_title}, ... ];
   ```
   `tide_title` は `data/tide.json` から日付で引いた潮名(大潮/中潮/小潮/長潮/若潮、無ければ`null`)。`docs/data.js` を書き換えたら `docs/index.html` と `docs/analysis.html` 両方の `data.js?v=N` のキャッシュバスターも1つ上げる

   **`GENERATED_AT`と`LAST_CHECKED_AT`は役割が違うので、新規レコードが0件の実行でも両方とも忘れずに更新すること**:
   - `window.GENERATED_AT` = 直近で新規レコードが実際に追加された時刻。新規0件の回はこの値を変更しない
   - `window.LAST_CHECKED_AT` = このパイプラインが最後に全ソースをチェックした時刻。**新規0件でも毎回この行だけは書き換える**(サイト側で「最終更新: {GENERATED_AT}(最終確認: {LAST_CHECKED_AT}・新着なし)」のように表示され、新着が無い日でもパイプラインが生きていることが利用者に伝わる)。新規レコードがあった回は`GENERATED_AT`と同じ時刻にしてよい(その場合サイト側は最終確認の注記を出さない)
   新規0件でも`LAST_CHECKED_AT`更新のためにdocs/data.jsは変更されるので、キャッシュバスターも上げてcommit・pushすること(「変更なしのためスキップ」にしない)
11. 各ソースの取得成否・新規件数・エラーの有無を簡潔に記録する(このファイルの末尾の「実行ログ」節に追記、直近10回分だけ残して古いものは削除してよい)
12. 変更を `git add -A && git commit -m "..." && git push` でリポジトリのmainブランチに反映する(GitHub Pagesが `docs/` を自動で再公開する)。`LAST_CHECKED_AT`の更新分だけであっても、他に変更が本当に何もない場合を除いてcommit・pushする

**注記(2026-08-08時点): 予約可否(空席状況)の自動抽出は廃止**。以前は`data/availability.json`として船宿ごとの空席状況を数時間おきに洗い替えする仕組みを試したが、(a) 16船宿中11船宿でしか拾えず、(b) 船宿ごとに表現形式が全く違い正規化の信頼性が低く、(c) 本質的にパイプラインの実行間隔(数時間おき)より短い周期で古くなる情報だったため、ユーザーの判断でこの自動抽出とその表示機能は削除した。代わりに `docs/funayado.html`(船宿情報ページ、場所・電話番号などほぼ不変の情報のみ)を静的に用意する方針にした。日次パイプラインでこのファイルに触れる必要は無い。

## 個別ソースの注意点(data/sources.jsonのparse_notesと重複するが要点のみ)
- ひらい丸・吉野屋・進丸・宮川丸・伊藤遊船: WordPressのRSS(`/feed/`)が使える。RSSは直近10〜20件程度しか含まれないため、既存データと突き合わせて「まだ取り込んでいない投稿」だけ処理すれば十分(通常は前回実行からの差分のみ)。
- 川崎丸: RSSが不安定なため `/blog/` のHTML一覧を直接取得してパースする。

## エラー時の扱い
- 特定の船宿サイトが落ちている・構造が変わってパースできない場合は、そのソースだけスキップし、他のソースの処理は続行する。スキップした場合はその旨を実行ログに残す。
- 1つのソースでパースに失敗するレコードがあっても、そのレコードだけ捨てて他のレコードは取り込む。

## 新しい船宿を追加する場合
`data/sources.json` の `sources` 配列に追記し、`phase2_candidates` から昇格させる。追加直後の初回実行では、その船宿だけ過去1〜2ヶ月分をバックフィル取得する。

## 実行ログ
(このセクションはスケジュールタスク実行のたびに追記される)
- 2026-08-08(手動実行、比較表の説明文を簡潔化): ユーザーから比較表上部の凡例段落について「説明がごちゃごちゃ長いのでシンプルにして」と指摘(直前のコミットで追加した凡例文が−/要確認/半・1日・午前午後/魚種統合ルール/料金変動注記を1段落に詰め込んだ結果600文字超になっていた)。要点(基本(男性/大人)料金を表示・−=便なし・要確認=料金未確認・半/1日/午前/午後=便により料金が異なる場合の併記・料金は要問合せ)のみ残し、魚種名統合ルールの詳細説明や「(半日/1日で価格差あり、詳細は公式サイト参照)」のような注記例の文言は削除して2文程度に短縮した。表そのもの(セル内容・列構成)は無変更。CSS/キャッシュバスターの変更なし。
- 2026-08-08(手動実行、お問い合わせページを新設): ユーザーから「いったんお問い合わせページ作って。送信したら私のkakeru.senoo@gmail.comにメールがくるようにして。」と依頼。本サイトはGitHub Pages上の静的サイト(サーバーサイド処理なし)のため、フォーム送信をメール転送するバックエンドとして外部サービスFormSubmit(formsubmit.co、無料・アカウント登録不要)を採用し、新規`docs/contact.html`を作成した。フォームは`action="https://formsubmit.co/kakeru.senoo@gmail.com"`のPOSTで、`_subject`(件名固定文言)・`_template=table`(整形されたテーブル形式のメール本文)・`_captcha=true`(スパム対策の簡易キャプチャを維持)・`_next`(送信後に`contact.html?sent=1`へリダイレクト)・ハニーポット用の非表示`_honey`フィールドをhiddenで指定。入力項目はお名前(任意)・メールアドレス(必須)・件名(任意)・お問い合わせ内容(必須)の4項目。送信後リダイレクト先の`?sent=1`をページ内の小さな無依存スクリプト(`data.js`/`common.js`等は一切読み込まない完全静的ページのため、この1箇所のみインラインJS)で検知し「✅ 送信しました」バナーを表示する。`docs/style.css`に`.contact-layout`/`.contact-form`/`.contact-field`/`.contact-required`/`.contact-submit`/`.contact-sent`を新規追加(既存の`.panel`・`.date-range input`・`.gear-banner`のトーンに合わせた配色)。`docs/index.html`・`docs/analysis.html`・`docs/funayado.html`の`<nav class="page-nav">`に4つ目のタブ`✉️ お問い合わせ`を追加し、4ファイルとも`style.css?v=28→29`に統一。`docs/sitemap.xml`に`contact.html`のエントリ(priority 0.3、changefreq yearly)を追加。Playwrightで、(1)`contact.html`単体を390px/1000pxで表示しナビ4タブ・アクティブタブ・フォームの`action`/`method`/`required`属性・横スクロール無しを確認、(2)`?sent=1`付きURLで完了バナーが表示されることを確認、(3)他3ページのナビにも4つ目のタブが反映されていることを確認。**重要な注記(手動確認が必要)**: FormSubmitは初回送信時、宛先メールアドレス(kakeru.senoo@gmail.com)宛てにアクティベーション確認メールを送信する仕様のため、実際にフォームが動作するには本人がそのメールを一度開いて確認リンクをクリックする必要がある(この手順はAIエージェントからは実行不可、運営者本人の対応が必要)。それまでの間の送信はFormSubmit側で保留され、メールとして転送されない。
- 2026-08-08(手動実行、「今日のおすすめ仕掛け」バナーをやめて控えめなカードに変更): ユーザーから全幅オレンジ帯バナー(ヘッダー直下・ナビ上)について「目立っているのにクリックしたいと思わない」とフィードバック。原因は(1)「今日の」と謳いながら中身は表示中の絞り込み条件からの汎用おすすめで日付と無関係、(2)広告然とした全幅の色帯で押し付けがましい、の2点と判断。対策として全幅バナー(`.gear-banner`)を廃止し、実際に表示中で最も報告の多い魚種1つと、その代表商品(画像+商品名)を出す小さいカード「gear-teaser」に置き換えた。配置もヘッダー直下から、左側フィルターパネルの末尾(検索条件の下)に変更し、広告というより関連コンテンツウィジェットに見えるようにした。実装: `common.js`に`computeTopSpecies(n)`を切り出し(`renderGearRecommendations`と共有)、新規`renderGearTeaser(containerId)`を追加(GEAR_PRODUCTSの画像付き商品を優先、無ければGEAR_RECOMMENDATIONSの検索キーワード、対象魚種が無ければ何も出さない)。`app.js`・`analysis.js`の`render()`内で`T.renderGearTeaser('gearTeaser')`を追加呼び出し。`index.html`(左サイドバーaside末尾)・`analysis.html`(1つ目のフィルターパネル末尾)に`<div id="gearTeaser" class="gear-teaser-slot">`を設置し、旧`<a class="gear-banner">`を削除。`style.css`から`.gear-banner`関連ルールを削除し、`.gear-teaser`系(サムネイル42px・魚種名・商品名+矢印)を新規追加。style.css?v=29→30、common.js?v=7→8、app.js?v=24→25、analysis.js?v=4→5に統一。Playwrightでanalysis.html(390px/1100px)にてタチウオ+商品画像+商品名のカードが実データから正しく生成されることを確認(index.htmlはサンドボックス内でLeaflet CDNがブロックされ`app.js`のrender()がその手前で例外停止するため未検証だが、`gearCards`など既存機能も同様に空になっており、これは今回の変更と無関係な既知のサンドボックス制約であることを確認済み)。
- 2026-08-08(手動実行、最終確認時刻の表示追加とアクセスカウンター新設): ユーザーから2件依頼。(1)「今8/7の18:05が最終更新になってるが13:00更新されてなくない?」との指摘に対し調査したところ、実際には2026-08-08 13:12(JST、UTC 04:12のcommit`112b5f5`)にパイプラインが15ソース(伊藤遊船は403で恒久除外)を全てチェック済みだが新規レコードが0件だったため`docs/data.js`(`GENERATED_AT`)を意図的に更新していなかっただけと判明。表示上「壊れているように見える」問題を解消するため、`docs/data.js`に`window.LAST_CHECKED_AT`(パイプラインが最後に全ソースをチェックした時刻、新規0件でも毎回更新)を新設し、`GENERATED_AT`(直近で新規レコードが実際に追加された時刻)と役割分担。`common.js`の`renderHeaderMeta`で、両者が異なる場合のみ「最終更新: {GENERATED_AT}(最終確認: {LAST_CHECKED_AT}・新着なし)」と表示するよう変更(同じ場合は従来通り「最終更新: X」のみ)。今回`LAST_CHECKED_AT`の初期値は調査結果に基づき"2026-08-08 13:12"を設定。`PIPELINE.md`の全体の流れに手順を追記し、新規0件の日でも`LAST_CHECKED_AT`更新のためdocs/data.jsの変更・cache buster更新・commit/pushを毎回行うよう明記(今後の日次実行が「変更なしのためスキップ」しないように)。(2)「アクセス数を掲載して」との依頼には、AskUserQuestionで実装方式を確認し「簡易カウンター(登録不要)」を選択。外部の無料カウンターAPI abacus.jasoncameron.dev(`GET /hit/{namespace}/{key}`でJSON `{value:N}`を返し訪問のたびに+1、CORS許可・登録不要、旧CountAPI.xyzは現在到達不能なため不採用)を採用し、新規`docs/counter.js`を作成(namespace="tokyo-bay-fishing-kakeru110"、key="total-pageviews"、ページ読み込みごとにhitを送り`id="pageViewCount"`へ返り値を表示するだけの十数行のスクリプト。同一訪問者の重複防止は行わない素朴な累計ヒット数)。4ページ(index/analysis/funayado/contact)全てのheaderに`<span class="meta">累計アクセス数: <span id="pageViewCount">-</span>回</span>`を追加し、`<script src="counter.js?v=1">`を読み込み。data.js?v=18→19、common.js?v=7... 実際はv=8→9(前回タスクでv=8化済み)に更新。Playwrightでanalysis.htmlの`lastUpdated`表示が新ロジック通りになることを確認。counter.jsのfetch自体は、curlでの直接検証(abacus.jasoncameron.dev、Origin付きリクエストで`access-control-allow-origin: *`を確認、複数回hitして値がインクリメントすることも確認)で仕組みが動作することは確認したが、このサンドボックスのPlaywright(ヘッドレスブラウザ)はLeaflet/Chart.js等と同様に許可リスト外の外部ドメインへの通信がブロックされるため、ブラウザ内でのfetch成功までは検証できていない(GitHub Pages上の実際の閲覧では通常のインターネットアクセスがあるため動作する見込み)。
- 2026-08-08(手動実行、PCでは魚種/船宿フィルターを開いた状態にする): ユーザーから「PCで開く場合は魚種、船宿の選択肢は閉じてなくてよい」と依頼。魚種/船宿の`<details class="filter-details">`はモバイルでのコンパクトさのために元々閉じた状態がデフォルトだったが、PCでは画面が広く閉じておく必要性が薄いとの指摘。`common.js`の`initFilterUI`冒頭で`window.matchMedia('(min-width: 601px)').matches`が真の場合(既存CSSのモバイル用ブレークポイント`@media (max-width: 600px)`と対になる閾値)、`.filter-details`全要素に`.open = true`を設定するようにした。モバイル(600px以下)は従来通り閉じたまま。common.js?v=9→10に更新。Playwrightで390px(閉)/800px(開)の両方でindex.html・analysis.htmlともに意図通りになることを確認。
- 2026-08-08(自動実行、日次差分取得・当日2回目): 同日朝の自動実行(本ログ前々回相当、現在は10件ローテーションで削除済み)が全15ソース新規0件だったため、夕方に再度main最新化の上で全16ソースを確認(伊藤遊船は`/report/feed/``/report/`とも今回も403 Forbiddenで恒久ブロックのためスキップ、それ以外15ソースは`curl -A "Mozilla/5.0 ..." --compressed`でアクセス、川崎丸は`Accept`/`Accept-Language`/`Referer`付きのフルブラウザ風リクエストで200取得)。並列サブエージェント5組に分担させ(①ひらい丸/ひらの丸/鈴福丸/中山丸=ぎょさん系ChokaDetailテンプレート、②つり幸=同系だが自由記述本文、③吉野屋/進丸/宮川丸/第三あさなぎ丸=WordPress RSS、④黒川丸/岩田屋/川崎丸、⑤山下丸/平作丸/一郎丸)、各エージェントに既存`source_url`一覧との突き合わせに加え`data/catches.json`本文(funayado+date+species+qty)でのクロスチェックも指示した上で、集約後にさらに`(funayado, date, 正規化species, qty_min, qty_max)`によるコンテンツキーで最終重複排除を実施。結果、**鈴福丸の新規候補10件(ChokaDetail個別URL、07-20〜08-05のビシアジ船)は全件`data/catches.json`に既存(前回まで`/category/Choka/`まとめURLをsource_urlとして記録済み)と判明し、URL不一致だが内容一致のためdedup除外**(朝の実行ログと同じ既知の事象、source_urlのみに頼る突き合わせでは検出できないため今回はcontent-keyでのクロスチェックを必須にして正しく除外できた)。最終的に12船宿(ひらい丸2・ひらの丸2・中山丸3・つり幸10・吉野屋5・進丸4・第三あさなぎ丸1・黒川丸3・岩田屋1・川崎丸2・山下丸2・平作丸1・一郎丸3、宮川丸は新着が出船予定告知のみで新規0件)から新規39件を取り込み(`data/catches.json` 1890→1929件)。座標マッチ内訳: gazetteer 22件・gazetteer_compound 2件(山下丸「久里浜沖～鴨居沖」、一郎丸「鴨居・大貫・下浦沖」で「大貫」を「大貫沖」のprefix逆方向一致として追加許容)・fallback_port 15件。新規species_aliasを3件追加(`太刀魚`→タチウオ、`タチウオ(エサ)`→タチウオ、`LTアジ ショート便`→マアジ)。第三あさなぎ丸の新規タチウオ「猿島沖・台船西沖」は「台船西沖」がgazetteer未登録のため前回同様マッチ数1件(猿島沖のみ)の`gazetteer`扱いで確定登録は見送り。サブエージェントの一次抽出結果をレビューした際、山下丸・平作丸・一郎丸の3ソースで`qty_unit`が実際のHTML(`匹数`列、本文とも「〜匹」表記)と異なり誤って`尾`と抽出されていた計6件を、fetch済み生HTMLの再確認と当該船宿の既存レコードの単位実績(全て`匹`)に基づき`匹`へ修正してから登録した。`data/tide.json`は2026-08-30まで格納済みのため追記なし、`data/weather.json`は2026-08-08分をOpen-Meteoから新規取得(前回実行時は当日分が未提供だったが、夕方の再取得で取得可能になった)。`docs/data.js`を全件から再生成(`GENERATED_AT`/`LAST_CHECKED_AT`とも2026-08-08 09:35に更新)、`docs/index.html`・`docs/analysis.html`ともdata.jsキャッシュバスターをv=19→v=20に更新。実行ログはこの回から直近10回分のみ残すよう整理(2026-08-06〜08-08の古いエントリ12件を削除)。
- 2026-08-08(自動実行、日次差分取得・当日3回目): 前回(09:26 UTC実行、コミット02ab9f6)から約3時間後にmain最新化の上で全16ソースを確認(伊藤遊船は`/report/feed/`が今回も403 Forbiddenで恒久ブロックを再確認のためスキップ、それ以外15ソースはWebFetch、またはブロックされた場合は`curl -A "Mozilla/5.0 ..." --compressed`にフォールバックしてアクセス、川崎丸は`Accept`/`Accept-Language`/`Referer`付きのフルブラウザ風リクエストで200取得)。並列サブエージェント5組に分担させ(①ひらい丸/ひらの丸/鈴福丸/中山丸=ぎょさん系ChokaDetailテンプレート、②つり幸、③吉野屋/進丸/宮川丸/第三あさなぎ丸=WordPress RSS、④黒川丸/岩田屋/川崎丸/伊藤遊船、⑤山下丸/平作丸/一郎丸)、既存`source_url`一覧との突き合わせに加え山下丸・平作丸(blog.phpが単一URLで全件掲載されるためURL照合が効かない)は`data/catches.json`の`(funayado, date, species, qty_min, qty_max)`コンテンツキーでのクロスチェックを必須にして処理した。結果、4船宿から新規4件を取り込み(`data/catches.json` 1929→1933件): ひらい丸(2026-08-08 LTアジ仕立船 マアジ18〜28cm、数量記載なし)、鈴福丸(2026-08-08 ビシアジ船 アジ25〜40cm 4〜42匹)、岩田屋(2026-08-08 LTアジ 18〜27cm 20〜72尾)、第三あさなぎ丸(2026-07-31付の今季最終イサキ船投稿、本文が「数伸びました」等の定性表現のみで数値記載が無かったためsize_min/max・qty_min/maxは全てnullでraw_textのみ保持)。他11船宿(進丸・吉野屋・宮川丸・つり幸・ひらの丸・中山丸・黒川丸・山下丸・平作丸・一郎丸)は新規0件(一郎丸・川崎丸・黒川丸には新規URL自体はあったが、休船・お客様不在・出船中止・予約案内などの釣果データを含まない投稿のみだったためスキップ)。座標マッチ内訳: 新規4件は全てground_textの記載が無く`fallback_port`(船宿の港座標を採用)。新規species_aliasの追加は無し(`アジ`→マアジ、`LTアジ`→マアジは既存の別名辞書でカバー済み)。`data/tide.json`は2026-08-30まで、`data/weather.json`は2026-08-08分まで既に格納済みのため新規レコードの日付(2026-08-08、2026-07-31)は両方とも追記不要だった。`docs/data.js`を全件から再生成(`GENERATED_AT`/`LAST_CHECKED_AT`とも2026-08-08 12:21に更新)、`docs/index.html`・`docs/analysis.html`ともdata.jsキャッシュバスターをv=20→v=21に更新。実行ログはこの回から直近10回分のみ残すよう先頭の最古エントリ1件を削除。
- 2026-08-08(手動実行、モバイルのナビタブが2行に割れる問題を修正): ユーザーがiPhoneのスクリーンショットを添付し「釣果実績とかが2段になって見にくい」と指摘。原因はお問い合わせページ追加でナビタブが3個→4個になったのに`.page-nav`が`flex-wrap`無しの1行flexのままだったため、狭い画面で各`<a>`が強制的に縮められ「釣果実\n績」のように文字の途中で改行されていた。`style.css`の`@media (max-width: 600px)`内で`.page-nav`に`flex-wrap: wrap`、各`.page-nav a`に`flex: 1 1 calc(50% - 6px)`・`white-space: nowrap`・`text-align: center`を追加し、モバイルでは2×2グリッドで各タブが1行に収まるようにした(角丸も上のみだったのを全体角丸に変更、2段になったので「パネルに繋がったタブ」の見た目は維持できないため)。style.css?v=30→31に更新。Playwrightで390px幅において全4タブの高さが均一(35px、折り返し無し)であることを確認、スクリーンショットでも1行ずつきれいに収まっていることを目視確認。
- 2026-08-09(自動実行、日次差分取得): mainを最新化の上、並列サブエージェント6組に分担させ全16ソースを確認(①つり幸/ひらの丸/鈴福丸/中山丸=ぎょさん系、②ひらい丸/吉野屋=WordPress RSS、③進丸/宮川丸/第三あさなぎ丸=WordPress RSS、④黒川丸/岩田屋/川崎丸、⑤山下丸/平作丸/一郎丸、⑥伊藤遊船単独の疎通確認)、各エージェントに既存`source_url`との突き合わせに加え`data/catches.json`のコンテンツキー(funayado+date+species+qty)でのクロスチェックも指示。伊藤遊船は`https`/`http`/`www`いずれもcurl・WebFetchとも403 Forbiddenで、7/25以降続く恒久ブロックを本日も再確認しスキップ。結果、2船宿から新規6件を取り込み(`data/catches.json` 1933→1939件): 宮川丸(2026-08-09 ルアー乗合船 タチウオ 65〜122cm 8〜40匹、場所「富津沖」はfishing_grounds.jsonに既存キーがあり`gazetteer`一致)、吉野屋(2026-08-09付の投稿post-954が魚種名5件(LTアジ/ふぐ/タチウオ/シロギス/マダコ)のみの記載で、サイズ・尾数・釣り場は本文が空欄の早朝スタブ投稿だったため、指示通り数値を捏造せず全てnullのまま5レコードとして登録、`ground_text`が無いため`fallback_port`(浦安)を採用)。他13船宿(つり幸・ひらの丸・鈴福丸・中山丸・ひらい丸・進丸・第三あさなぎ丸・黒川丸・岩田屋・川崎丸・山下丸・平作丸・一郎丸)は新規0件(鈴福丸は前回同様ChokaDetail個別URL9件が候補に挙がったがいずれも既存レコードとコンテンツキー一致でdedup、川崎丸は新規URL4件・一郎丸は新規URL1件がいずれも休船/お客様不在の告知のみで釣果データなしのためスキップ)。新規species_aliasの追加は無し(`LTアジ`→マアジ、`タチウオ`は既存の辞書・慣例でカバー済み。「ふぐ」は吉野屋が従来から使っている表記で、既存データでも正規化せずそのまま`species`として扱われているため今回もそのまま登録)。新規fishing_grounds追加も無し。`data/tide.json`は2026-08-30まで格納済みのため追記不要、`data/weather.json`は2026-08-09分がOpen-Meteoで本日から取得可能になっていたため新規追加(tmax 30.8℃/tmin 24.7℃/降水0.1mm/風17.1kmh/瞬間43.2kmh/気圧1007.5hPa/weathercode 51)。`docs/data.js`を全件から再生成(`GENERATED_AT`/`LAST_CHECKED_AT`とも2026-08-09 13:20に更新)、`docs/index.html`・`docs/analysis.html`ともdata.jsキャッシュバスターをv=21→v=22に更新。実行ログはこの回から直近10回分のみ残すよう先頭の最古エントリ2件を削除。
- 2026-08-09(自動実行、日次差分取得・当日2回目): 前回(04:20 UTC=13:20 JST実行、コミット81db5d4)から約5時間後にmainを最新化の上、並列サブエージェント6組に分担させ全16ソースを確認(①つり幸/ひらの丸/鈴福丸/中山丸=ぎょさん系、②ひらい丸/吉野屋=WordPress RSS、③進丸/宮川丸/第三あさなぎ丸=WordPress RSS、④黒川丸/岩田屋/川崎丸、⑤山下丸/平作丸/一郎丸、⑥伊藤遊船単独の疎通確認)、各エージェントに既存source_urlとの突き合わせに加えdata/catches.jsonのコンテンツキー(funayado+date+species+qty+ground_text)でのクロスチェックも指示。伊藤遊船は https/http/www いずれもWebFetch・curlとも403 Forbiddenで、7/25以降続く恒久ブロックを本日2回目も再確認しスキップ。結果、9船宿から新規32件を取り込み(data/catches.json 1939→1971件): つり幸11件(305264/305265/305267/305268の4詳細ページ、午前アジ2便・カサゴ/カマス数量不明外道2件・午前シロギス2便・太刀魚3便(天秤/ルアー混在)・午後アジ2便)、ひらい丸4件(feed_url自体が404で機能停止していたためトップページ+ChokaDetail直接スクレイピングにフォールバック、BBQ仕立船アジ・タチウオ乗合船2便(天秤/テンヤ)・マダコ乗合船)、進丸4件(2026-08-09投稿、半日午前/午後のシロギス・アジ計4レコード、場所記載なしのためfallback_port)、第三あさなぎ丸1件(太刀魚船、猿島沖からスタート、species_raw「太刀魚船」を新規エイリアスとしてタチウオに正規化)、中山丸2件(LTアジ船=本牧〜富岡沖、タチウオ船=猿島〜横須賀沖、いずれもgazetteer_compound)、黒川丸3件(Atomフィードの生XMLをcurlで取得、リレータチ天秤/テンヤ+リレーアジ、7/22の新規URLは仕立船予約案内のみで釣果データなしスキップ)、川崎丸2件(WebFetchで/blog/一覧取得、curl+ブラウザヘッダで個別記事取得、タチウオ・マダコ各1件、台風13号うねり等による出船中止の新規URL3件はスキップ)、山下丸2件(単一URL一覧のためsource_urlでなくrecent_content_keysでdedupe、カワハギ・アジ)、一郎丸3件(新規detail ID2件、タチウオ・アジリレーからタチウオ+アジ2件、ふぐ釣りから1件、species_raw「ふぐ」を新規エイリアスとしてフグに正規化)。他5船宿(宮川丸・ひらの丸・鈴福丸・岩田屋・平作丸)は新規0件(宮川丸は最新投稿が既に前回登録済み、鈴福丸は候補9件が全てcontent-keyで既存レコードと一致し重複除外=URL体系変更が原因と推定、平作丸も同様に最新投稿が既存content-keyと完全一致)。新規species_alias 2件追加(「太刀魚船」→タチウオ、「ふぐ」→フグ)。新規fishing_grounds追加は無し(全ての地名が既存gazetteerで解決)。data/tide.jsonは2026-08-30まで、data/weather.jsonは2026-08-09分まで既に格納済みのため両方とも追記不要だった(新規レコードは全て2026-08-09付)。docs/data.jsを全件から再生成(GENERATED_AT/LAST_CHECKED_ATとも2026-08-09 18:14に更新)、docs/index.html・docs/analysis.htmlともdata.jsキャッシュバスターをv=22→v=23に更新。実行ログはこの回から直近10回分のみ残すよう先頭の最古エントリ1件を削除。
